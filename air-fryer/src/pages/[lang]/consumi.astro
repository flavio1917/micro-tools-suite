---
import MainLayout from '../../layouts/MainLayout.astro';
import Disclaimer from '../../components/Disclaimer.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'it' } },
    { params: { lang: 'en' } },
    { params: { lang: 'es' } },
    { params: { lang: 'fr' } }
  ];
}

const lang = Astro.params.lang || 'it';
const homeUrl = `/${lang}/#ricettario`;

const backBtnLabel = {
  it: "Vai alle ricette",
  en: "Go to Recipes",
  es: "Ir a las recetas",
  fr: "Voir les recettes"
}[lang] || "Vai alle ricette";

const content = {
  it: {
    title: "Calcolatore Consumi Friggitrice ad Aria: Quanto costa in bolletta?",
    h1: "Il Simulatore di Bolletta",
    desc: "Basta bollette a sorpresa! Muovi i cursori per scoprire istantaneamente quanto ti costa accendere la friggitrice rispetto al forno tradizionale.",
    liveBadge: "🔴 LIVE",
    wLabel: "Potenza Friggitrice (Watt)",
    mLabel: "Minuti di Cottura",
    cLabel: "Costo Energia (€/kWh)",
    resAF: "Costo Friggitrice",
    resOven: "Costo Forno Elettrico (2500W)*",
    resSave: "Soldi Risparmiati",
    ovenNote: "*Il forno tradizionale richiede circa il 25% di tempo in più per eguagliare la cottura ad aria."
  },
  en: {
    title: "Air Fryer Electricity Cost Calculator vs Conventional Oven",
    h1: "Electricity Bill Simulator",
    desc: "No more surprise bills! Move the sliders to instantly discover how much it costs to run your air fryer compared to a traditional oven.",
    liveBadge: "🔴 LIVE",
    wLabel: "Air Fryer Power (Watts)",
    mLabel: "Cooking Minutes",
    cLabel: "Energy Cost (€/kWh)",
    resAF: "Air Fryer Cost",
    resOven: "Electric Oven Cost (2500W)*",
    resSave: "Money Saved",
    ovenNote: "*A traditional oven requires about 25% more time to match air frying results."
  },
  es: {
    title: "Calculadora de Consumo Eléctrico: Freidora de Aire vs Horno",
    h1: "Simulador de Factura",
    desc: "¡Se acabaron las sorpresas en la factura! Mueve los controles para descubrir al instante cuánto cuesta usar la freidora frente al horno.",
    liveBadge: "🔴 LIVE",
    wLabel: "Potencia Freidora (Vatios)",
    mLabel: "Minutos de Cocción",
    cLabel: "Coste Energía (€/kWh)",
    resAF: "Coste Freidora",
    resOven: "Coste Horno Eléctrico (2500W)*",
    resSave: "Dinero Ahorrado",
    ovenNote: "*El horno tradicional requiere un 25% más de tiempo para igualar la cocción por aire."
  },
  fr: {
    title: "Calculateur de Consommation Électrique : Friteuse à Air vs Four",
    h1: "Simulateur de Facture",
    desc: "Fini les factures surprises ! Déplacez les curseurs pour découvrir instantanément combien coûte votre friteuse par rapport au four.",
    liveBadge: "🔴 LIVE",
    wLabel: "Puissance Friteuse (Watts)",
    mLabel: "Minutes de Cuisson",
    cLabel: "Coût de l'Énergie (€/kWh)",
    resAF: "Coût Friteuse",
    resOven: "Coût Four Électrique (2500W)*",
    resSave: "Argent Économisé",
    ovenNote: "*Un four traditionnel nécessite environ 25 % de temps en plus pour égaler la cuisson à l'air."
  }
};

const t = content[lang as keyof typeof content] || content.it;

const softwareSchema = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": t.h1,
  "operatingSystem": "All",
  "applicationCategory": "UtilitiesApplication",
  "description": t.desc,
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "EUR" }
};
---

<MainLayout title={t.title} lang={lang}>
  <section class="max-w-4xl mx-auto py-12 md:py-16 px-4">
    
    <div class="mb-8 flex justify-start">
      <a href={homeUrl} class="inline-flex items-center justify-center px-5 py-2.5 bg-slate-800/90 backdrop-blur-md text-white border border-slate-600/50 shadow-xl rounded-full hover:bg-slate-700 transition-all z-10 group">
        <svg class="w-4 h-4 mr-2 flex-shrink-0 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
        <span class="font-bold text-sm">{backBtnLabel}</span>
      </a>
    </div>

    <header class="text-center mb-12">
      <div class="inline-flex items-center justify-center gap-2 px-3 py-1 bg-red-500/10 border border-red-500/30 rounded-full mb-4 animate-pulse">
        <span class="w-2 h-2 rounded-full bg-red-500"></span>
        <span class="text-[10px] font-black text-red-400 tracking-widest uppercase">{t.liveBadge}</span>
      </div>
      <h1 class="text-3xl md:text-5xl font-black text-white mb-4 tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-amber-500">
        {t.h1}
      </h1>
      <p class="text-slate-400 text-base md:text-xl max-w-2xl mx-auto leading-relaxed">{t.desc}</p>
    </header>

    <div class="bg-slate-800/60 border border-slate-700 rounded-3xl p-6 md:p-10 shadow-2xl mb-12 backdrop-blur-sm max-w-3xl mx-auto relative overflow-hidden flex flex-col lg:flex-row gap-10">
      <div class="absolute -top-24 -left-24 w-64 h-64 bg-amber-500/10 rounded-full blur-3xl pointer-events-none"></div>

      <div class="flex-1 space-y-8 relative z-10">
        
        <div class="bg-slate-900/50 p-5 rounded-2xl border border-slate-700/50">
          <div class="flex justify-between items-end mb-4">
            <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">{t.wLabel}</label>
            <div class="text-2xl font-black font-mono text-yellow-400"><span id="val-watts">1500</span> <span class="text-sm text-yellow-500">W</span></div>
          </div>
          <input type="range" id="slider-watts" min="800" max="2500" step="50" value="1500" class="w-full h-2 bg-slate-700 rounded-lg appearance-none cursor-pointer accent-yellow-500" />
          <div class="flex justify-between text-[10px] text-slate-500 mt-2 font-mono font-bold"><span>800W</span><span>2500W</span></div>
        </div>

        <div class="bg-slate-900/50 p-5 rounded-2xl border border-slate-700/50">
          <div class="flex justify-between items-end mb-4">
            <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">{t.mLabel}</label>
            <div class="text-2xl font-black font-mono text-blue-400"><span id="val-mins">20</span> <span class="text-sm text-blue-500">min</span></div>
          </div>
          <input type="range" id="slider-mins" min="1" max="120" step="1" value="20" class="w-full h-2 bg-slate-700 rounded-lg appearance-none cursor-pointer accent-blue-500" />
          <div class="flex justify-between text-[10px] text-slate-500 mt-2 font-mono font-bold"><span>1 min</span><span>120 min</span></div>
        </div>

        <div class="bg-slate-900/50 p-5 rounded-2xl border border-slate-700/50">
          <div class="flex justify-between items-center">
            <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">{t.cLabel}</label>
            <div class="relative w-28">
              <input type="number" id="input-cost" value="0.25" step="0.01" min="0.05" class="w-full bg-slate-800 border border-slate-600 text-white rounded-lg px-3 py-2 focus:ring-2 focus:ring-amber-500 outline-none text-right font-mono font-bold text-lg" />
            </div>
          </div>
        </div>

      </div>

      <div class="flex-1 relative z-10 flex flex-col justify-center">
        
        <div class="space-y-6">
          <div>
            <div class="flex justify-between items-end mb-2">
              <span class="text-xs font-bold text-green-400 uppercase tracking-wider">{t.resAF}</span>
              <div class="text-3xl font-black font-mono text-white"><span id="res-af">0.00</span> <span class="text-green-500 text-lg">€</span></div>
            </div>
            <div class="w-full bg-slate-800 rounded-full h-3 overflow-hidden border border-slate-700">
              <div id="bar-af" class="bg-gradient-to-r from-green-500 to-emerald-400 h-full rounded-full transition-all duration-300 ease-out" style="width: 40%"></div>
            </div>
          </div>

          <div>
            <div class="flex justify-between items-end mb-2">
              <span class="text-xs font-bold text-red-400 uppercase tracking-wider">{t.resOven}</span>
              <div class="text-3xl font-black font-mono text-white opacity-70"><span id="res-oven">0.00</span> <span class="text-red-500 text-lg">€</span></div>
            </div>
            <div class="w-full bg-slate-800 rounded-full h-3 overflow-hidden border border-slate-700">
              <div class="bg-gradient-to-r from-red-600 to-rose-500 h-full rounded-full w-full opacity-70"></div>
            </div>
          </div>
        </div>
        
        <div class="mt-8 bg-gradient-to-br from-amber-500/20 to-orange-500/10 border border-amber-500/40 rounded-2xl p-6 text-center transform hover:scale-[1.02] transition-transform">
          <span class="block text-xs uppercase font-bold text-amber-400 tracking-wider mb-2">{t.resSave}</span>
          <div class="flex items-baseline justify-center gap-1">
            <span class="text-amber-400 font-bold text-2xl">-</span>
            <span id="res-save" class="text-5xl font-black font-mono text-white drop-shadow-md">0.00</span>
            <span class="text-amber-500 font-bold text-2xl">€</span>
          </div>
        </div>
        
        <p class="text-[10px] text-slate-500 mt-6 text-center italic leading-relaxed">{t.ovenNote}</p>
      </div>
    </div>

    <Disclaimer type="generale" lang={lang} />

  </section>

  <script is:inline>
    document.addEventListener('astro:page-load', () => {
      const sliderWatts = document.getElementById('slider-watts');
      const valWatts = document.getElementById('val-watts');
      const sliderMins = document.getElementById('slider-mins');
      const valMins = document.getElementById('val-mins');
      const inCost = document.getElementById('input-cost');
      
      const elAF = document.getElementById('res-af');
      const elOven = document.getElementById('res-oven');
      const elSave = document.getElementById('res-save');
      const barAF = document.getElementById('bar-af');

      // Se non trova lo slider, ferma lo script (evita errori in altre pagine)
      if (!sliderWatts) return; 

      function calculateLive() {
        const watts = parseFloat(sliderWatts.value);
        const mins = parseFloat(sliderMins.value);
        const costKwh = parseFloat(inCost.value) || 0;

        valWatts.textContent = watts;
        valMins.textContent = mins;

        const costAF = (watts / 1000) * (mins / 60) * costKwh;
        const ovenWatts = 2500;
        const ovenMins = mins * 1.25;
        const costOven = (ovenWatts / 1000) * (ovenMins / 60) * costKwh;
        const savings = costOven - costAF;

        elAF.textContent = costAF.toFixed(2);
        elOven.textContent = costOven.toFixed(2);
        elSave.textContent = Math.max(0, savings).toFixed(2);

        const ratio = Math.min((costAF / costOven) * 100, 100);
        barAF.style.width = `${ratio}%`;
      }

      sliderWatts.addEventListener('input', calculateLive);
      sliderMins.addEventListener('input', calculateLive);
      inCost.addEventListener('input', calculateLive);

      calculateLive();
    });
  </script>

  <script type="application/ld+json" set:html={JSON.stringify(softwareSchema)} />
</MainLayout>